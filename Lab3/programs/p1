begin
in number a;
in number b;
in number c;
var number maxim;

is (a>=b) and (a>=c) ? then maxim=a end;
is (b>=a) and (b>=c) ? then maxim=b end;
is (c>=a) and (c>=b) ? then maxim=c end;

out "Maximul celor trei numere este:";
out maxim;
endd.

begin
in number n;
var bool isPrim;

is (n <= 1) ? then isPrim = False end;

loop i start 2 stop n
    is (n % i == 0) ? then isPrim = False
			else isPrim = True end;
sfarsit;

is (isPrim == True) ? then out "Este prim"
			  else out "Nu is prim" end;
endd.
